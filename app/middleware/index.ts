import bundle from './bundle';
import render from './render';
import router from './router';
import staticMv from './static';

export default {
    bundle,
    render,
    router,
    static: staticMv,
};
